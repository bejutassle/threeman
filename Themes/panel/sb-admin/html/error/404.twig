{% extends "content.twig" %}
{% block content %}

<div class="container-fluid">

  <!-- 404 Error Text -->
  <div class="text-center">
    <div class="error mx-auto" data-text="404">404</div>
    <p class="lead text-gray-800 mb-5">{{'admin.404_title'|t}}</p>
    <p class="text-gray-500 mb-0">{{'admin.404_text'|t({'url': url('')})}}</p>
    <a href="{{url('')}}">← {{'admin.404_back_dashboard'|t}}</a>
  </div>

</div>

{% endblock %}
